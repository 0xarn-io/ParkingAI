# --- Core API / server ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- Vision / geometry ---
# On a Raspberry Pi / headless server prefer opencv-python-headless instead
# (it has no GUI deps). opencv-python is only needed for the zone editor's
# cv2.imshow window on a desktop.
opencv-python>=4.9
numpy>=1.26
shapely>=2.0
PyYAML>=6.0

# --- AI detector (PyTorch + YOLO) ---
# ultralytics pulls in torch/torchvision. On first run it downloads the
# yolov8n.pt weights (~6 MB). On a Pi, export to NCNN for a big speedup
# (see README "Raspberry Pi notes").
ultralytics>=8.1
